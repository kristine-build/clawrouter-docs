# 获取Stripe支付金额 | New API

管理接口充值

# 获取Stripe支付金额

🔐 需要登录（User权限）



/`api`/`user`/`stripe`/`amount`


## [Response Body](#response-body)

### 200

cURLJavaScriptGoPythonJavaC#

```
curl -X POST "https://docs.newapi.pro/api/user/stripe/amount"
```

200

Empty
